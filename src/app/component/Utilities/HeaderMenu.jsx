const HeaderMenu = ({title}) => {
  return (
    <section>
      <div className="p-12">
        <h1 className="text-center text-4xl">{title}</h1>
      </div>
    </section>
  );
};

export default HeaderMenu;
